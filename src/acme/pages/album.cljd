(ns acme.pages.album
  (:require
   [acme.pages.album-list :as albums-list]
   ["package:flutter/material.dart" :as m]))
;;
;; [cljd.flutter :as f]))

;;
;;
;;

(def main
  (m/Scaffold
   .appBar (m/AppBar .title (m/Text "Fetch Data List Example"))
   .body
   (m/Center
    .child
    (albums-list/view))))
